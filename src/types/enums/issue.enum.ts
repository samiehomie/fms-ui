export enum IssueType {
  HARDWARE = 'hardware',
  SOFTWARE = 'software',
}

export enum IssueStatus {
  SUBMITTED = 'submitted',
  IN_PROGRESS = 'in_progress',
  RESOLVED = 'resolved',
  CLOSED = 'closed',
  REJECTED = 'rejected',
}

export enum IssuePriority {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
  CRITICAL = 'critical',
}
